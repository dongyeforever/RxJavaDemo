<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tk.dongyeblog.rxjavademo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.BehaviorSubjectAty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.TokenAdvanceAty" />
        <activity android:name=".ui.TokenActivity" />
        <activity android:name=".ui.ZipActivity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.MapActivity" />
        <activity android:name=".ui.RxSimpleDemo" />
        <activity android:name=".simple.ComposeExampleActivity" />
        <activity android:name=".simple.SchedulerMultiExampleActivity" />
        <activity android:name=".simple.RxOperatorDemo" />
        <activity android:name=".simple.MapActivity" />

    </application>

</manifest>